<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.nhuchhe.nischit.bbrowser.MainActivity"
    android:background="@drawable/background"
    >

    <Button
        android:id="@+id/create_bubble"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create Bubble"
        android:padding="25dp"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <TextView
        android:id="@+id/helpText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/black"
        android:textColor="@color/white"
        android:padding="10dp"
        android:text="1. Tap green bar to minimize the browser.\n2. Drag the bubble to the bottom of the screen to remove."
        android:layout_above="@+id/create_bubble"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        android:layout_marginBottom="16dp"
        android:layout_above="@id/helpText"
        android:textSize="32sp"
        android:typeface="monospace"
        android:layout_centerHorizontal="true"
        android:text="BBROWSER"/>
    <ImageView
        android:id="@+id/rating_imageView"
        android:layout_width="42dp"
        android:layout_height="42dp"
        android:src="@drawable/star"
        android:layout_above="@+id/textView"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="90dp" />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="25dp"
        android:background="@android:color/holo_green_light"
        android:layout_alignParentBottom="true"
        android:textColor="@android:color/white"
        android:gravity="center"
        android:text="Drag here to remove"
        android:id="@+id/textView" />


</RelativeLayout>
